{alias, sample, "./sample/"}.
{alias, state, "./state/"}.
{alias, groups, "./groups/"}.
{logdir, "./logs/"}.
 
{suites, sample, all}.
{suites, state, all}.
{suites, groups, all}.
{skip_cases, sample, sample_SUITE, test2, "This test fails on purpose"}.
